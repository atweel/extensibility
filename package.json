{
  "name": "@stackeat/extensibility",
  "version": "0.1.2",
  "description": "> TODO: description",
  "author": "Eduard Malakhov <eduard@stackeat.com>",
  "homepage": "https://github.com/stackeat/extensibility#readme",
  "license": "ISC",
  "main": "lib/exports.js",
  "directories": {
    "lib": "lib"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stackeat/extensibility.git"
  },
  "scripts": {
    "build": "ttsc --build",
    "watch": "ttsc --build --watch",
    "clean": "rm -r lib",
    "rebuild": "yarn clean && yarn build",
    "prepublishOnly": "yarn rebuild && yarn version --patch",
    "postpublish": "git add package.json && git commit -m'Update package version to $(jq --raw-output .version package.json)'"
  },
  "bugs": {
    "url": "https://github.com/stackeat/extensibility/issues"
  },
  "devDependencies": {
    "@zerollup/ts-transform-paths": "^1.7.3",
    "ttypescript": "^1.5.7",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "inversify": "^5.0.1"
  }
}
